package CA_2; // Defines the package this enum belongs to

// Enum declaration for different menu options in the application
public enum MenuOption {
    SORT,        // Option to sort employee records
    SEARCH,      // Option to search for an employee record
    ADD_RECORDS, // Option to add new employee records
    EXIT         // Option to exit the application
}
