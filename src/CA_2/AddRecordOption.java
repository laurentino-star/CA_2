// Declares the package this enum belongs to (CA_2)
package CA_2;

// Defines an enum for different options when adding records
public enum AddRecordOption {
    // Represents the option to manually add a new employee record
    ADD_EMPLOYEE,
    
    // Represents the option to automatically generate a random employee record
    GENERATE_RANDOM_EMPLOYEE
}
